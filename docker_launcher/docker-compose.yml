version: '3'

services:
  db:
    image: mysql
    environment:
      MYSQL_DATABASE: mysql_db
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"

  web:
    tty: yes
    build: .
    volumes:
      - ../web_codes:/code
    ports:
      - "8000:8000"
    depends_on:
      - db
    environment:
      WAIT_HOSTS: db:3306
